{
  "name": "zenn",
  "context": "..",
  "dockerComposeFile": "docker-compose.yml",
  "service": "zenn",
  "appPort": [
    8000
  ],
  "postCreateCommand": "apt-get update; apt-get install -y git; git config --global include.path /zenn/.gitconfig"
}
